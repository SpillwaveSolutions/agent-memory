---
phase: 31-gemini-cli-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli/fixtures/gemini/session-start.json
  - tests/cli/fixtures/gemini/session-end.json
  - tests/cli/fixtures/gemini/before-agent.json
  - tests/cli/fixtures/gemini/after-agent.json
  - tests/cli/fixtures/gemini/before-tool.json
  - tests/cli/fixtures/gemini/after-tool.json
  - tests/cli/fixtures/gemini/malformed.json
  - tests/cli/gemini/smoke.bats
  - tests/cli/gemini/hooks.bats
autonomous: true

must_haves:
  truths:
    - "Gemini fixture JSON files exist in Gemini-native format (BeforeAgent with .prompt, AfterAgent with .prompt_response)"
    - "smoke.bats validates binary detection, daemon health, basic ingest, and graceful gemini CLI skip"
    - "hooks.bats feeds Gemini-format JSON into memory-capture.sh and verifies translation to CchEvent format via gRPC query"
    - "Hook script tests assert exit 0 and {} output (not {\"continue\":true})"
  artifacts:
    - path: "tests/cli/fixtures/gemini/before-agent.json"
      provides: "Gemini BeforeAgent fixture with .prompt field"
      contains: "BeforeAgent"
    - path: "tests/cli/fixtures/gemini/after-agent.json"
      provides: "Gemini AfterAgent fixture with .prompt_response field"
      contains: "AfterAgent"
    - path: "tests/cli/gemini/smoke.bats"
      provides: "GEMI-01 smoke tests"
      min_lines: 50
    - path: "tests/cli/gemini/hooks.bats"
      provides: "GEMI-02 hook capture tests"
      min_lines: 100
  key_links:
    - from: "tests/cli/gemini/smoke.bats"
      to: "tests/cli/lib/common.bash"
      via: "load '../lib/common'"
      pattern: "load.*lib/common"
    - from: "tests/cli/gemini/hooks.bats"
      to: "plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh"
      via: "HOOK_SCRIPT variable"
      pattern: "memory-capture\\.sh"
---

<objective>
Create Gemini CLI fixture files and the first two test files (smoke.bats, hooks.bats) that validate binary detection, daemon health, basic ingest, and the memory-capture.sh hook translation layer.

Purpose: Covers requirements GEMI-01 (smoke tests) and GEMI-02 (hook capture tests) -- the core of Gemini CLI E2E testing.
Output: 7 fixture JSON files + 2 bats test files (~16-20 tests)
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gemini-cli-tests/31-RESEARCH.md
@tests/cli/lib/common.bash
@tests/cli/lib/cli_wrappers.bash
@tests/cli/claude-code/smoke.bats
@tests/cli/claude-code/hooks.bats
@tests/cli/fixtures/claude-code/session-start.json
@plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gemini fixture JSON files</name>
  <files>
    tests/cli/fixtures/gemini/session-start.json
    tests/cli/fixtures/gemini/session-end.json
    tests/cli/fixtures/gemini/before-agent.json
    tests/cli/fixtures/gemini/after-agent.json
    tests/cli/fixtures/gemini/before-tool.json
    tests/cli/fixtures/gemini/after-tool.json
    tests/cli/fixtures/gemini/malformed.json
  </files>
  <action>
Create `tests/cli/fixtures/gemini/` directory and 7 fixture files in Gemini-native hook JSON format. These fixtures use Gemini event names (BeforeAgent, AfterAgent, etc.) NOT CchEvent names.

CRITICAL differences from Claude Code fixtures:
- Use `"hook_event_name": "BeforeAgent"` (not "UserPromptSubmit")
- Use `"hook_event_name": "AfterAgent"` (not "AssistantResponse")
- Use `"hook_event_name": "BeforeTool"` / `"AfterTool"` (not "PreToolUse" / "PostToolUse")
- Use `"hook_event_name": "SessionEnd"` (not "Stop")
- BeforeAgent has `.prompt` field (not `.message`)
- AfterAgent has `.prompt_response` field (not `.message`)

All fixtures MUST be compact single-line JSON (jq -c format) because memory-ingest reads stdin line-by-line and multi-line JSON silently fails.

Use session_id "gemini-test-001" as placeholder (tests rewrite it). Use timestamp "2026-02-25T10:00:00Z". Use cwd "/tmp/test-workspace".

Fixture content (use exact schemas from RESEARCH.md "Gemini-Format Fixture JSON Schemas" section but output as single-line):
1. session-start.json: SessionStart with source "startup"
2. session-end.json: SessionEnd with reason "exit"
3. before-agent.json: BeforeAgent with prompt "What is the project structure?"
4. after-agent.json: AfterAgent with prompt_response "The project contains src/ and tests/ directories."
5. before-tool.json: BeforeTool with tool_name "read_file", tool_input {"file_path": "/tmp/test-workspace/README.md"}
6. after-tool.json: AfterTool with tool_name "read_file", tool_input {"file_path": "/tmp/test-workspace/README.md"}, tool_response {"content": "# README"}
7. malformed.json: `{not valid json` (same pattern as claude-code/malformed.json)
  </action>
  <verify>
    Run: `ls tests/cli/fixtures/gemini/*.json | wc -l` should output 7.
    Run: `jq empty tests/cli/fixtures/gemini/before-agent.json` should succeed.
    Run: `jq -r '.hook_event_name' tests/cli/fixtures/gemini/before-agent.json` should output "BeforeAgent".
    Run: `jq -r '.prompt' tests/cli/fixtures/gemini/before-agent.json` should output "What is the project structure?".
    Run: `wc -l < tests/cli/fixtures/gemini/before-agent.json` should output 1 (single line).
  </verify>
  <done>7 Gemini-format fixture files exist, all valid JSON (except malformed.json), all single-line, all using Gemini event names and field conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke.bats and hooks.bats for Gemini CLI</name>
  <files>
    tests/cli/gemini/smoke.bats
    tests/cli/gemini/hooks.bats
  </files>
  <action>
Create `tests/cli/gemini/` directory and two bats test files. Mirror the structure of `tests/cli/claude-code/smoke.bats` and `tests/cli/claude-code/hooks.bats` but adapted for Gemini.

**smoke.bats** (GEMI-01, ~8 tests):
- Load `'../lib/common'` and `'../lib/cli_wrappers'`
- setup_file: `build_daemon_if_needed`, `setup_workspace`, `start_daemon`
- teardown_file: `stop_daemon`, `teardown_workspace`
- Test 1: memory-daemon binary exists and is executable
- Test 2: memory-ingest binary exists and is executable
- Test 3: daemon is running and healthy (`assert_daemon_running`, `daemon_health_check`)
- Test 4: memory-capture.sh exists and is executable (check `${PROJECT_ROOT}/plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh`)
- Test 5: memory-ingest produces continue:true on valid CchEvent JSON (use direct CchEvent with agent "gemini", NOT Gemini-format fixture)
- Test 6: memory-ingest produces continue:true on malformed JSON (pipe malformed.json from gemini fixtures)
- Test 7: gemini binary detection works (skip if not installed) -- `require_cli gemini "Gemini CLI"` then `run gemini --version`
- Test 8: gemini help shows hook-related flags (skip if not installed) -- `require_cli gemini "Gemini CLI"` then `run gemini --help` and check output is non-empty

**hooks.bats** (GEMI-02, ~8-10 tests):
- Load `'../lib/common'` and `'../lib/cli_wrappers'`
- setup_file: `build_daemon_if_needed`, `setup_workspace`, `start_daemon`
- teardown_file: `stop_daemon`, `teardown_workspace`
- Set `FIXTURE_DIR="${PROJECT_ROOT}/tests/cli/fixtures/gemini"` at file scope
- Set `HOOK_SCRIPT="${PROJECT_ROOT}/plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh"` at file scope

Helpers (define at top of hooks.bats, same pattern as claude-code/hooks.bats):
- `rewrite_session_id()`: jq -c to rewrite session_id (same as claude-code version)
- `query_all_events()`: `run grpc_query events --from 0 --to 9999999999999 --limit 1000; echo "$output"`

CRITICAL: Hook script tests use TWO-LAYER pattern:
  Layer 1: Feed Gemini-format JSON into memory-capture.sh via stdin with env vars:
    `run bash -c "echo '$json' | MEMORY_INGEST_PATH='${MEMORY_INGEST_BIN}' MEMORY_DAEMON_ADDR='http://127.0.0.1:${MEMORY_DAEMON_PORT}' '$HOOK_SCRIPT'"`
    Assert: `[ "$status" -eq 0 ]` and `[[ "$output" == '{}' ]]`
    NOTE: Gemini hook outputs `{}` NOT `{"continue":true}`
  Layer 2: Query gRPC and verify event was stored:
    `result="$(query_all_events)"`
    Assert content appears in result.

CRITICAL: The hook script runs memory-ingest in BACKGROUND (`&`), so add `sleep 2` between Layer 1 and Layer 2 to allow async ingest to complete.

Hook tests:
- Test 1: SessionStart event captured via hook script. Assert `[[ "$result" != *"No events found"* ]]`
- Test 2: BeforeAgent event captures prompt. Assert `[[ "$result" == *"project structure"* ]]`
- Test 3: AfterAgent event captures response. Assert `[[ "$result" == *"src/ and tests/"* ]]`
- Test 4: BeforeTool event captures tool name. Assert `[[ "$result" == *"tool:"* ]]`
- Test 5: AfterTool event captures tool name. Assert `[[ "$result" == *"tool:"* ]]`
- Test 6: SessionEnd event maps to Stop. Assert event count increased (no "No events found")
- Test 7: Multiple events in sequence maintain session coherence. Ingest SessionStart + BeforeAgent + AfterAgent + SessionEnd for same session_id, verify prompt and response content appear.
- Test 8: Hook script with ANSI-contaminated input still works. Create a fixture string with embedded ANSI escape (e.g., `$'\x1b[32m'` prefixed to the JSON), feed to hook script, assert exit 0 and `{}` output.

All assertions use hard `false` on failure (not `|| true`), with diagnostic echo before the false statement (following Phase 30-06 pattern).
  </action>
  <verify>
    Run: `bats --count tests/cli/gemini/smoke.bats` should output 8 (validates syntax).
    Run: `bats --count tests/cli/gemini/hooks.bats` should output 8 (validates syntax).
    Run: `bats tests/cli/gemini/smoke.bats` should pass all non-skip tests.
    Run: `bats tests/cli/gemini/hooks.bats` should pass all tests.
  </verify>
  <done>smoke.bats passes 6+ tests (2 may skip if gemini not installed). hooks.bats passes all 8 tests with two-layer proof (Layer 1: hook exit 0 + {}, Layer 2: gRPC query confirms storage).</done>
</task>

</tasks>

<verification>
- `ls tests/cli/fixtures/gemini/*.json | wc -l` outputs 7
- `bats tests/cli/gemini/smoke.bats` -- all non-skip tests pass
- `bats tests/cli/gemini/hooks.bats` -- all tests pass
- No shared helpers modified (zero changes to common.bash or cli_wrappers.bash)
- All hook tests assert `{}` output (not `{"continue":true}`)
</verification>

<success_criteria>
- 7 Gemini fixture files exist in correct format
- smoke.bats covers GEMI-01 (binary detection, ingest, daemon health, graceful skip)
- hooks.bats covers GEMI-02 (all 6 Gemini event types translated via memory-capture.sh)
- All tests pass when run via `bats tests/cli/gemini/`
</success_criteria>

<output>
After completion, create `.planning/phases/31-gemini-cli-tests/31-01-SUMMARY.md`
</output>
