---
phase: 31-gemini-cli-tests
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - tests/cli/gemini/pipeline.bats
  - tests/cli/gemini/negative.bats
autonomous: true

must_haves:
  truths:
    - "pipeline.bats proves the full ingest-to-query cycle with agent=gemini events"
    - "Pipeline tests use direct CchEvent ingest (bypassing hook script) for deterministic timing"
    - "negative.bats proves daemon-down, malformed-input, and fail-open behavior for both memory-ingest and memory-capture.sh"
    - "Negative tests for memory-capture.sh assert exit 0 and {} output in all failure modes"
    - "Negative tests for memory-ingest assert exit 0 and {\"continue\":true} in all failure modes"
    - "Running bats tests/cli/gemini/ executes all 4 test files successfully"
  artifacts:
    - path: "tests/cli/gemini/pipeline.bats"
      provides: "GEMI-03 E2E pipeline tests"
      min_lines: 80
    - path: "tests/cli/gemini/negative.bats"
      provides: "GEMI-04 negative tests"
      min_lines: 80
  key_links:
    - from: "tests/cli/gemini/pipeline.bats"
      to: "tests/cli/lib/common.bash"
      via: "ingest_event helper"
      pattern: "ingest_event"
    - from: "tests/cli/gemini/negative.bats"
      to: "plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh"
      via: "HOOK_SCRIPT variable for fail-open tests"
      pattern: "memory-capture\\.sh"
---

<objective>
Create pipeline.bats (E2E ingest-to-query cycle) and negative.bats (error handling, fail-open) for Gemini CLI, completing all 4 requirements (GEMI-03, GEMI-04).

Purpose: Proves the full Gemini pipeline works end-to-end and that all failure modes are handled gracefully without test leaks.
Output: 2 bats test files (~11-13 tests)
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-gemini-cli-tests/31-RESEARCH.md
@.planning/phases/31-gemini-cli-tests/31-01-SUMMARY.md
@tests/cli/lib/common.bash
@tests/cli/lib/cli_wrappers.bash
@tests/cli/claude-code/pipeline.bats
@tests/cli/claude-code/negative.bats
@plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline.bats for Gemini E2E pipeline tests</name>
  <files>tests/cli/gemini/pipeline.bats</files>
  <action>
Create `tests/cli/gemini/pipeline.bats` mirroring `tests/cli/claude-code/pipeline.bats` but with `"agent": "gemini"` in all events.

- Load `'../lib/common'` and `'../lib/cli_wrappers'`
- setup_file: `build_daemon_if_needed`, `setup_workspace`, `start_daemon`
- teardown_file: `stop_daemon`, `teardown_workspace`

Include `_now_ms()` helper (same as claude-code version -- python3 fallback to seconds*1000).

Include `_ingest_full_session()` helper that ingests a 6-event Gemini session using DIRECT CchEvent format (already-translated events, NOT Gemini-native format). This tests the storage layer, not the translation layer. All events use `"agent": "gemini"`. Events: SessionStart, UserPromptSubmit ("What is 2+2?"), PreToolUse (Read /test.rs), PostToolUse (Read /test.rs), AssistantResponse ("The answer is 4."), Stop.

Pipeline tests (GEMI-03, ~5-6 tests):
- Test 1: Complete session lifecycle via hook ingest. Ingest full 6-event session, sleep 2, query events in time window, assert "6 found", assert "What is 2+2?" and "The answer is 4." in output.
- Test 2: Ingested events are queryable via TOC browse. `run grpc_query root`, assert status 0 and non-empty output.
- Test 3: Events with cwd metadata are stored correctly. Ingest SessionStart with specific cwd, query time window, assert event found.
- Test 4: Gemini agent field is preserved through ingest. Ingest a UserPromptSubmit with agent "gemini" and message "Hello from Gemini pipeline", sleep 1, query_all_events (from 0 to 9999999999999), assert `[[ "$result" == *"gemini:"* ]]` OR `[[ "$result" == *"Hello from Gemini pipeline"* ]]`.
- Test 5: Concurrent sessions maintain isolation. Two sessions with unique marker messages, interleaved, assert both markers appear and "6 found" in time-windowed query.

Use hard assertions with diagnostic echo (not `|| true`). Same pattern as claude-code/pipeline.bats.
  </action>
  <verify>
    Run: `bats --count tests/cli/gemini/pipeline.bats` should output 5.
    Run: `bats tests/cli/gemini/pipeline.bats` should pass all tests.
  </verify>
  <done>pipeline.bats passes all 5 tests proving the complete Gemini ingest-to-query cycle works with agent=gemini events.</done>
</task>

<task type="auto">
  <name>Task 2: Create negative.bats for Gemini error handling tests</name>
  <files>tests/cli/gemini/negative.bats</files>
  <action>
Create `tests/cli/gemini/negative.bats` covering GEMI-04. This tests BOTH memory-ingest fail-open and memory-capture.sh fail-open behavior.

- Load `'../lib/common'` and `'../lib/cli_wrappers'`
- setup_file: `build_daemon_if_needed`, `setup_workspace` (daemon NOT started -- tests manage it explicitly)
- teardown_file: `stop_daemon 2>/dev/null || true`, `teardown_workspace`
- Set `FIXTURE_DIR="${BATS_TEST_DIRNAME}/../fixtures/gemini"` at file scope
- Set `HOOK_SCRIPT="${PROJECT_ROOT}/plugins/memory-gemini-adapter/.gemini/hooks/memory-capture.sh"` at file scope

Negative tests (GEMI-04, ~7 tests):

memory-ingest fail-open tests (assert `{"continue":true}`):
- Test 1: memory-ingest with daemon down still returns continue:true. Use unused random port, pipe SessionStart JSON with agent "gemini" directly to MEMORY_INGEST_BIN.
- Test 2: memory-ingest with malformed JSON returns continue:true. Pipe malformed.json fixture to MEMORY_INGEST_BIN.
- Test 3: memory-ingest with empty stdin returns continue:true. Pipe empty string to MEMORY_INGEST_BIN.
- Test 4: memory-ingest with unknown event type returns continue:true. Pipe JSON with `"hook_event_name":"UnknownEventType"` and `"agent":"gemini"`.

memory-capture.sh fail-open tests (assert `{}` output):
- Test 5: memory-capture.sh with daemon down still returns {}. Use unused random port, pipe Gemini-format SessionStart JSON into HOOK_SCRIPT with `MEMORY_INGEST_PATH='${MEMORY_INGEST_BIN}' MEMORY_DAEMON_ADDR='http://127.0.0.1:${unused_port}'`. Assert exit 0 and output `{}`.
- Test 6: memory-capture.sh with malformed input still returns {}. Pipe `{not valid json` into HOOK_SCRIPT. Assert exit 0 and output `{}`.
- Test 7: memory-capture.sh with empty stdin still returns {}. Pipe empty string into HOOK_SCRIPT. Assert exit 0 and output `{}`.

All assertions use hard `false` on failure with diagnostic echo. Use `run bash -c "..."` pattern for piping into binaries (same as claude-code/negative.bats).

IMPORTANT: Test 5 needs to set MEMORY_INGEST_PATH to the actual binary (otherwise hook script might find a system memory-ingest or fail to find one). Test 6 and 7 also need MEMORY_INGEST_PATH set even though they won't reach ingest (the script guards on jq parse / empty input before attempting ingest).
  </action>
  <verify>
    Run: `bats --count tests/cli/gemini/negative.bats` should output 7.
    Run: `bats tests/cli/gemini/negative.bats` should pass all tests.
    Run: `bats tests/cli/gemini/` should pass all tests across all 4 files.
  </verify>
  <done>negative.bats passes all 7 tests proving graceful error handling for both memory-ingest and memory-capture.sh. Running `bats tests/cli/gemini/` executes all Gemini tests successfully.</done>
</task>

</tasks>

<verification>
- `bats --count tests/cli/gemini/pipeline.bats` outputs 5
- `bats --count tests/cli/gemini/negative.bats` outputs 7
- `bats tests/cli/gemini/` passes all tests (~28-30 total across 4 files)
- No shared helpers modified (zero changes to common.bash or cli_wrappers.bash)
- Pipeline tests use `"agent": "gemini"` in all events
- Negative tests cover both memory-ingest and memory-capture.sh fail-open paths
</verification>

<success_criteria>
- pipeline.bats covers GEMI-03 (full ingest-to-query cycle with agent=gemini)
- negative.bats covers GEMI-04 (daemon-down, malformed, fail-open for both binaries)
- All 4 Gemini test files pass when run together via `bats tests/cli/gemini/`
- Phase 31 success criteria met: all 4 requirements (GEMI-01 through GEMI-04) covered
</success_criteria>

<output>
After completion, create `.planning/phases/31-gemini-cli-tests/31-02-SUMMARY.md`
</output>
