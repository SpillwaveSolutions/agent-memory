---
phase: 61-operate-it
plan: 01
type: execute
wave: 1
depends_on: [60-01]
files_modified:
  - crates/memory-storage/src/db.rs
  - crates/memory-indexing/src/pipeline.rs
  - crates/memory-indexing/src/lib.rs
  - crates/memory-daemon/src/cli.rs
  - crates/memory-daemon/src/commands.rs
  - crates/e2e-tests/src/lib.rs
  - crates/e2e-tests/tests/backfill_test.rs
  - docs/UPGRADING.md
  - README.md
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "admin backfill-index --index bm25|vector|all [--from-sequence N] [--batch 500] [--dry-run]"
    - "refuses to run if the daemon PID is live; error names memory-daemon stop"
    - "second run reports 0 new documents"
    - "--dry-run prints counts and writes nothing"
    - "max_batches=1 then resume equals SIGINT-after-batch-1"
    - "--from-sequence 0 replays the event log when the outbox is empty"
  artifacts:
    - path: crates/e2e-tests/tests/backfill_test.rs
      provides: 50-event backfill then TeleportSearch previews
      contains: backfillneedle
---

<objective>
Stopped-daemon `admin backfill-index` that replays events into BM25 so pre-v3.1 empty `text_preview`s can be filled. Resumable, idempotent, dry-run, event-log fallback.
</objective>

<context>
Issue #41 / v3.2 plan 61-01. `rebuild-indexes` is TOC/grips. `rebuild-bm25` is a prune.
Import.rs was not cherry-picked (not on this line); event-log iteration is the replay path.
Default start resumes from IndexCheckpoint. `--from-sequence 0` is the empty-preview force-reindex.
</context>
