---
phase: 32-opencode-cli-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli/fixtures/opencode/session-start.json
  - tests/cli/fixtures/opencode/user-prompt.json
  - tests/cli/fixtures/opencode/assistant-response.json
  - tests/cli/fixtures/opencode/post-tool-use.json
  - tests/cli/fixtures/opencode/stop.json
  - tests/cli/fixtures/opencode/malformed.json
  - tests/cli/lib/cli_wrappers.bash
  - tests/cli/opencode/smoke.bats
  - tests/cli/opencode/hooks.bats
autonomous: true

must_haves:
  truths:
    - "Running bats tests/cli/opencode/smoke.bats executes smoke tests with daemon lifecycle and graceful skip for missing opencode binary"
    - "Running bats tests/cli/opencode/hooks.bats ingests all 5 OpenCode event types via memory-ingest with agent=opencode and verifies storage via gRPC"
    - "OpenCode fixture JSON files exist as compact single-line CchEvent format with agent=opencode"
    - "run_opencode wrapper in cli_wrappers.bash provides timeout-guarded headless invocation"
  artifacts:
    - path: "tests/cli/fixtures/opencode/session-start.json"
      provides: "SessionStart CchEvent fixture with agent=opencode"
    - path: "tests/cli/fixtures/opencode/user-prompt.json"
      provides: "UserPromptSubmit CchEvent fixture with agent=opencode"
    - path: "tests/cli/fixtures/opencode/assistant-response.json"
      provides: "AssistantResponse CchEvent fixture with agent=opencode"
    - path: "tests/cli/fixtures/opencode/post-tool-use.json"
      provides: "PostToolUse CchEvent fixture with agent=opencode"
    - path: "tests/cli/fixtures/opencode/stop.json"
      provides: "Stop CchEvent fixture with agent=opencode"
    - path: "tests/cli/fixtures/opencode/malformed.json"
      provides: "Intentionally broken JSON for negative/smoke tests"
    - path: "tests/cli/opencode/smoke.bats"
      provides: "8 smoke tests (binary check, daemon health, ingest validation, opencode CLI skip)"
    - path: "tests/cli/opencode/hooks.bats"
      provides: "7 hook capture tests (all 5 event types + sequence + agent field)"
  key_links:
    - from: "tests/cli/opencode/smoke.bats"
      to: "tests/cli/lib/common.bash"
      via: "load '../lib/common'"
      pattern: "load.*lib/common"
    - from: "tests/cli/opencode/hooks.bats"
      to: "tests/cli/fixtures/opencode/*.json"
      via: "rewrite_session_id reads fixture files"
      pattern: "FIXTURE_DIR.*fixtures/opencode"
    - from: "tests/cli/opencode/hooks.bats"
      to: "memory-ingest binary"
      via: "ingest_event helper from common.bash"
      pattern: "ingest_event"
---

<objective>
Create OpenCode CLI fixture JSON files, add run_opencode wrapper to cli_wrappers.bash, and implement smoke.bats + hooks.bats test files.

Purpose: Establish the OpenCode test foundation -- fixtures for all 5 CchEvent types (no PreToolUse in OpenCode), smoke tests for binary detection and basic ingest, and hook capture tests proving all event types are stored via gRPC.

Output: 6 fixture JSON files, updated cli_wrappers.bash, 2 bats test files (~15 tests total)
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-opencode-cli-tests/32-RESEARCH.md

# Reference patterns from Gemini (mirror structure)
@tests/cli/gemini/smoke.bats
@tests/cli/gemini/hooks.bats
@tests/cli/lib/common.bash
@tests/cli/lib/cli_wrappers.bash
@tests/cli/fixtures/gemini/session-start.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenCode fixture JSON files and run_opencode wrapper</name>
  <files>
    tests/cli/fixtures/opencode/session-start.json
    tests/cli/fixtures/opencode/user-prompt.json
    tests/cli/fixtures/opencode/assistant-response.json
    tests/cli/fixtures/opencode/post-tool-use.json
    tests/cli/fixtures/opencode/stop.json
    tests/cli/fixtures/opencode/malformed.json
    tests/cli/lib/cli_wrappers.bash
  </files>
  <action>
    Create 6 fixture files in `tests/cli/fixtures/opencode/`. Each MUST be compact single-line JSON (memory-ingest reads line-by-line; multi-line JSON silently fails). All fixtures use `"agent": "opencode"`.

    **OpenCode has only 5 event types (NO PreToolUse):**

    1. `session-start.json`: `{"hook_event_name":"SessionStart","session_id":"opencode-test-001","timestamp":"2026-02-26T10:00:00Z","cwd":"/tmp/test-workspace","agent":"opencode"}`
    2. `user-prompt.json`: `{"hook_event_name":"UserPromptSubmit","session_id":"opencode-test-001","timestamp":"2026-02-26T10:00:05Z","message":"What is the project structure?","agent":"opencode"}`
    3. `assistant-response.json`: `{"hook_event_name":"AssistantResponse","session_id":"opencode-test-001","timestamp":"2026-02-26T10:00:15Z","message":"The project contains src/ and tests/ directories.","agent":"opencode"}`
    4. `post-tool-use.json`: `{"hook_event_name":"PostToolUse","session_id":"opencode-test-001","timestamp":"2026-02-26T10:00:10Z","tool_name":"read_file","tool_input":{"file_path":"/tmp/test-workspace/README.md"},"agent":"opencode"}`
    5. `stop.json`: `{"hook_event_name":"Stop","session_id":"opencode-test-001","timestamp":"2026-02-26T10:00:30Z","cwd":"/tmp/test-workspace","agent":"opencode"}`
    6. `malformed.json`: `{not valid json at all -- this is intentionally broken`

    Then append `run_opencode` wrapper to `tests/cli/lib/cli_wrappers.bash`. Add it after the existing `run_claude_with_hooks` section, before the hook/ingest pipeline section. The wrapper should:
    - Use `opencode run --format json "$@"` syntax (current anomalyco/opencode, NOT old `-p -q -f json`)
    - Wrap in TIMEOUT_CMD with CLI_TIMEOUT
    - Capture stderr to TEST_WORKSPACE/opencode_stderr.log
    - Follow exact pattern of `run_claude` function

    ```bash
    # --- OpenCode wrappers ---

    run_opencode() {
        # Usage: run_opencode <prompt> [extra args...]
        # Wraps opencode CLI in headless mode with timeout and JSON output.
        local test_stderr="${TEST_WORKSPACE:-/tmp}/opencode_stderr.log"
        export TEST_STDERR="${test_stderr}"

        local cmd=("opencode" "run" "--format" "json" "$@")

        if [[ -n "${TIMEOUT_CMD}" ]]; then
            "${TIMEOUT_CMD}" "${CLI_TIMEOUT}s" "${cmd[@]}" 2>"${test_stderr}"
        else
            "${cmd[@]}" 2>"${test_stderr}"
        fi
    }
    ```
  </action>
  <verify>
    1. `ls tests/cli/fixtures/opencode/` shows 6 files
    2. `python3 -c "import json; json.loads(open('tests/cli/fixtures/opencode/session-start.json').read())"` succeeds (valid JSON)
    3. `wc -l tests/cli/fixtures/opencode/session-start.json` shows 1 (single line)
    4. `grep -c 'run_opencode' tests/cli/lib/cli_wrappers.bash` returns at least 1
    5. `grep '"agent":"opencode"' tests/cli/fixtures/opencode/*.json | wc -l` returns 5 (all valid fixtures)
  </verify>
  <done>6 compact single-line CchEvent fixture files exist with agent=opencode, run_opencode wrapper appended to cli_wrappers.bash</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke.bats and hooks.bats for OpenCode</name>
  <files>
    tests/cli/opencode/smoke.bats
    tests/cli/opencode/hooks.bats
  </files>
  <action>
    Create `tests/cli/opencode/smoke.bats` mirroring Gemini smoke.bats with these OpenCode-specific differences:
    - No hook script test (OpenCode uses TypeScript plugin, not bash script). Replace Gemini's "memory-capture.sh exists" test with "memory-capture.ts plugin file exists" test that checks `plugins/memory-opencode-plugin/.opencode/plugin/memory-capture.ts`
    - Binary detection tests use `require_cli opencode "OpenCode"` and skip gracefully
    - Headless smoke test uses `run_opencode "echo hello"` with timeout guard; if timeout (exit 124/137), skip with message "OpenCode headless mode timed out (known quirk)"
    - 8 tests total:
      1. memory-daemon binary exists and is executable
      2. memory-ingest binary exists and is executable
      3. daemon is running and healthy
      4. memory-capture.ts plugin file exists
      5. memory-ingest produces continue:true on valid CchEvent JSON (with agent=opencode)
      6. memory-ingest produces continue:true on malformed JSON
      7. opencode binary detection works (skip if not installed)
      8. opencode headless mode produces output (skip if not installed) -- use timeout guard

    Use `setup_file` / `teardown_file` with `build_daemon_if_needed`, `setup_workspace`, `start_daemon`, `stop_daemon`, `teardown_workspace`.

    Create `tests/cli/opencode/hooks.bats` with these specifications:
    - Since OpenCode's TypeScript plugin cannot be invoked from shell, ALL hook tests use DIRECT CchEvent ingest via `ingest_event` helper (NOT piping through a hook script)
    - Two-layer proof pattern: Layer 1 = ingest_event returns exit 0 with continue:true. Layer 2 = gRPC query confirms event stored.
    - sleep 2 between ingest and gRPC query (background ingest timing)
    - Hard assertions: `[[ expr ]] || { echo "diagnostic"; false; }`
    - rewrite_session_id uses `jq -c` (compact output, critical for memory-ingest)
    - 7 tests:
      1. hook: SessionStart event is captured via direct ingest (opencode)
      2. hook: UserPromptSubmit event captures message (opencode)
      3. hook: AssistantResponse event captures response (opencode)
      4. hook: PostToolUse event captures tool name (opencode)
      5. hook: Stop event is captured (opencode)
      6. hook: multiple events in sequence maintain session coherence (5 events, no PreToolUse)
      7. hook: agent field "opencode" is preserved through ingest

    Key differences from Gemini hooks.bats:
    - NO `HOOK_SCRIPT` variable or pipe-through-script pattern
    - Layer 1 uses `run ingest_event "$json"` directly, checking for `"continue":true` (not `{}`)
    - Only 5 event types in sequence test (SessionStart, UserPromptSubmit, PostToolUse, AssistantResponse, Stop)
    - No ANSI test (that was Gemini hook-script specific)

    Define FIXTURE_DIR as `"${PROJECT_ROOT}/tests/cli/fixtures/opencode"` at file scope.

    Helper functions (define in hooks.bats like Gemini does):
    ```bash
    rewrite_session_id() {
      local fixture_file="$1"
      local new_sid="$2"
      if command -v jq &>/dev/null; then
        jq -c --arg sid "$new_sid" '.session_id = $sid' "$fixture_file"
      else
        sed "s/\"session_id\":[[:space:]]*\"[^\"]*\"/\"session_id\": \"${new_sid}\"/" "$fixture_file" | tr -d '\n'
      fi
    }

    query_all_events() {
      run grpc_query events --from 0 --to 9999999999999 --limit 1000
      echo "$output"
    }
    ```
  </action>
  <verify>
    1. `bats --count tests/cli/opencode/smoke.bats` returns 8
    2. `bats --count tests/cli/opencode/hooks.bats` returns 7
    3. `grep -c 'require_cli opencode' tests/cli/opencode/smoke.bats` returns 2 (two CLI-dependent tests)
    4. `grep -c 'ingest_event' tests/cli/opencode/hooks.bats` returns at least 5 (one per event type + sequence)
    5. `grep -c 'HOOK_SCRIPT' tests/cli/opencode/hooks.bats` returns 0 (no hook script piping)
    6. `grep -c '"agent":"opencode"' tests/cli/opencode/smoke.bats` returns at least 1
    7. `grep -c 'PreToolUse' tests/cli/opencode/hooks.bats` returns 0 (OpenCode has no PreToolUse)
  </verify>
  <done>smoke.bats (8 tests) and hooks.bats (7 tests) created for OpenCode, using direct CchEvent ingest pattern with hard assertions and two-layer proof</done>
</task>

</tasks>

<verification>
1. `bats --count tests/cli/opencode/smoke.bats` returns 8
2. `bats --count tests/cli/opencode/hooks.bats` returns 7
3. All fixture files are single-line compact JSON with agent=opencode
4. No references to PreToolUse in OpenCode tests
5. No HOOK_SCRIPT piping pattern in hooks.bats (TypeScript plugin not testable from shell)
6. cli_wrappers.bash contains run_opencode function
7. `bats tests/cli/opencode/smoke.bats` passes (daemon tests)
8. `bats tests/cli/opencode/hooks.bats` passes (all events stored via gRPC)
</verification>

<success_criteria>
- 15 tests across 2 bats files (8 smoke + 7 hooks) all passing
- 6 fixture JSON files created with correct CchEvent format
- run_opencode wrapper added to cli_wrappers.bash
- OpenCode binary tests skip gracefully when opencode not installed
- All hook tests use direct ingest (no hook script piping)
</success_criteria>

<output>
After completion, create `.planning/phases/32-opencode-cli-tests/32-01-SUMMARY.md`
</output>
