---
phase: 32-opencode-cli-tests
plan: 02
type: execute
wave: 1
depends_on: ["32-01"]
files_modified:
  - tests/cli/opencode/pipeline.bats
  - tests/cli/opencode/negative.bats
autonomous: true

must_haves:
  truths:
    - "Running bats tests/cli/opencode/pipeline.bats proves the complete ingest-to-query cycle with agent=opencode including 5-event session lifecycle"
    - "Running bats tests/cli/opencode/negative.bats proves fail-open behavior for daemon-down, malformed JSON, empty stdin, and unknown event types"
    - "Pipeline tests use direct CchEvent format (not Gemini/hook-script format) for deterministic storage layer testing"
    - "Negative tests cover memory-ingest fail-open only (no hook script layer since OpenCode uses TypeScript plugin)"
  artifacts:
    - path: "tests/cli/opencode/pipeline.bats"
      provides: "5 E2E pipeline tests (session lifecycle, TOC browse, cwd metadata, agent field, concurrent sessions)"
      min_lines: 100
    - path: "tests/cli/opencode/negative.bats"
      provides: "5-6 negative tests (daemon down, malformed, empty stdin, unknown event, timeout scenario)"
      min_lines: 60
  key_links:
    - from: "tests/cli/opencode/pipeline.bats"
      to: "memory-ingest + memory-daemon"
      via: "ingest_event + grpc_query from common.bash"
      pattern: "ingest_event.*opencode"
    - from: "tests/cli/opencode/negative.bats"
      to: "memory-ingest binary"
      via: "direct pipe to MEMORY_INGEST_BIN"
      pattern: "MEMORY_INGEST_BIN"
---

<objective>
Create pipeline.bats and negative.bats for OpenCode CLI E2E tests.

Purpose: Complete the 4-file test structure (smoke, hooks, pipeline, negative) by adding full ingest-to-query pipeline validation and fail-open negative tests specific to OpenCode.

Output: 2 bats test files (~10-11 tests total), completing OPEN-03 and OPEN-04 requirements
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-opencode-cli-tests/32-RESEARCH.md

# Reference patterns from Gemini (mirror structure)
@tests/cli/gemini/pipeline.bats
@tests/cli/gemini/negative.bats
@tests/cli/lib/common.bash

# Plan 01 summary for fixture paths and patterns
@.planning/phases/32-opencode-cli-tests/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline.bats for OpenCode E2E ingest-to-query tests</name>
  <files>tests/cli/opencode/pipeline.bats</files>
  <action>
    Create `tests/cli/opencode/pipeline.bats` mirroring Gemini pipeline.bats with OpenCode-specific differences.

    File header comment: "OpenCode CLI E2E pipeline tests -- full ingest -> query cycle (OPEN-03). Uses DIRECT CchEvent format with agent=opencode. OpenCode has 5 event types (no PreToolUse)."

    Load helpers: `load '../lib/common'` and `load '../lib/cli_wrappers'`

    setup_file/teardown_file: `build_daemon_if_needed`, `setup_workspace`, `start_daemon` / `stop_daemon`, `teardown_workspace`

    Helper `_now_ms()`: Same as Gemini -- python3 fallback to seconds*1000.

    Helper `_ingest_full_session()`: Takes session_id arg. Ingests 5 events (NOT 6 like Gemini -- no PreToolUse):
    1. SessionStart (with cwd)
    2. UserPromptSubmit ("What is 2+2?")
    3. PostToolUse (tool_name: "Read", tool_input: {path: "/test.rs"})
    4. AssistantResponse ("The answer is 4.")
    5. Stop
    All with `"agent":"opencode"` and timestamp from `date -u +%Y-%m-%dT%H:%M:%SZ`.

    5 tests:
    1. "pipeline: complete opencode session lifecycle via hook ingest" -- ingest 5 events, sleep 2, query time window, assert "5 found", assert "What is 2+2?" and "The answer is 4." in output
    2. "pipeline: opencode ingested events are queryable via TOC browse" -- `grpc_query root`, assert exit 0 and non-empty output
    3. "pipeline: opencode events with cwd metadata are stored correctly" -- ingest SessionStart with specific cwd, sleep 1, query, assert events found
    4. "pipeline: opencode agent field is preserved through ingest" -- ingest UserPromptSubmit with unique message, sleep 1, query wide window, assert "opencode:" or message content in output
    5. "pipeline: opencode concurrent sessions maintain isolation" -- interleave 2 sessions (3 events each: Start, UserPrompt, Stop), sleep 2, query, assert both unique messages found AND "6 found"

    Use hard assertions: `[[ expr ]] || { echo "diagnostic"; false; }`
  </action>
  <verify>
    1. `bats --count tests/cli/opencode/pipeline.bats` returns 5
    2. `grep -c '"agent":"opencode"' tests/cli/opencode/pipeline.bats` returns at least 5
    3. `grep -c 'PreToolUse' tests/cli/opencode/pipeline.bats` returns 0
    4. `grep -c '5 found' tests/cli/opencode/pipeline.bats` returns 1 (lifecycle test expects 5, not 6)
    5. `bats tests/cli/opencode/pipeline.bats` passes
  </verify>
  <done>pipeline.bats with 5 tests proving complete OpenCode ingest-to-query cycle with 5-event sessions</done>
</task>

<task type="auto">
  <name>Task 2: Create negative.bats for OpenCode fail-open and error handling tests</name>
  <files>tests/cli/opencode/negative.bats</files>
  <action>
    Create `tests/cli/opencode/negative.bats` mirroring Gemini negative.bats with OpenCode-specific differences.

    File header comment: "OpenCode CLI negative tests -- daemon down, malformed input, fail-open behavior (OPEN-04). Tests memory-ingest fail-open ONLY (no hook script layer -- OpenCode uses TypeScript plugin)."

    Load helpers: `load '../lib/common'` and `load '../lib/cli_wrappers'`

    setup_file/teardown_file: `build_daemon_if_needed`, `setup_workspace` / `stop_daemon 2>/dev/null || true`, `teardown_workspace`. NOTE: Daemon is NOT started in setup -- tests that need it start explicitly.

    FIXTURE_DIR: `"${BATS_TEST_DIRNAME}/../fixtures/opencode"`

    Key difference from Gemini negative.bats: OpenCode has NO hook script, so there are NO memory-capture.sh fail-open tests. Only memory-ingest fail-open tests (assert `{"continue":true}`). Replace the Gemini hook-script tests with an OpenCode-specific timeout negative test.

    5 tests (memory-ingest fail-open only):
    1. "negative: memory-ingest with daemon down still returns continue:true (opencode)" -- unused random port, pipe SessionStart JSON with agent=opencode, assert exit 0 + `{"continue":true}`
    2. "negative: memory-ingest with malformed JSON returns continue:true (opencode)" -- cat malformed.json fixture, assert exit 0 + `{"continue":true}`
    3. "negative: memory-ingest with empty stdin returns continue:true (opencode)" -- `echo ''`, assert exit 0 + `{"continue":true}`
    4. "negative: memory-ingest with unknown event type returns continue:true (opencode)" -- UnknownEventType with agent=opencode, assert exit 0 + `{"continue":true}`
    5. "negative: opencode headless timeout produces skip-friendly exit (skip if not installed)" -- `require_cli opencode "OpenCode"`, use TIMEOUT_CMD with very short timeout (2s) and dummy prompt, accept exit 0/124/137 as valid (all prove timeout mechanism works). If no timeout cmd, skip.

    All piped commands use format: `run bash -c "echo 'JSON' | MEMORY_DAEMON_ADDR=\"http://127.0.0.1:${unused_port}\" '${MEMORY_INGEST_BIN}'"` -- exactly matching Gemini pattern.

    Hard assertions throughout.
  </action>
  <verify>
    1. `bats --count tests/cli/opencode/negative.bats` returns 5
    2. `grep -c 'memory-capture' tests/cli/opencode/negative.bats` returns 0 (no hook script tests)
    3. `grep -c '"continue":true' tests/cli/opencode/negative.bats` returns 4 (all ingest fail-open tests)
    4. `grep -c 'require_cli opencode' tests/cli/opencode/negative.bats` returns 1 (timeout test only)
    5. `bats tests/cli/opencode/negative.bats` passes
  </verify>
  <done>negative.bats with 5 tests covering daemon-down, malformed, empty, unknown-event, and timeout scenarios for OpenCode</done>
</task>

</tasks>

<verification>
1. `bats --count tests/cli/opencode/pipeline.bats` returns 5
2. `bats --count tests/cli/opencode/negative.bats` returns 5
3. No PreToolUse references in either file
4. No hook script references in negative.bats
5. `bats tests/cli/opencode/pipeline.bats` passes
6. `bats tests/cli/opencode/negative.bats` passes
7. Full suite: `bats tests/cli/opencode/` runs all 25 tests (8+7+5+5)
</verification>

<success_criteria>
- 10 tests across 2 bats files (5 pipeline + 5 negative) all passing
- Pipeline tests prove 5-event OpenCode session lifecycle through ingest-to-query
- Negative tests prove fail-open for all error modes
- No hook script testing (TypeScript plugin not shell-testable)
- Full `bats tests/cli/opencode/` runs 25 total tests
</success_criteria>

<output>
After completion, create `.planning/phases/32-opencode-cli-tests/32-02-SUMMARY.md`
</output>
