---
phase: 38-e2e-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/e2e-tests/tests/fail_open_test.rs
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Events ingest successfully when embedder is disabled (fail-open for dedup gate)"
    - "All events are stored in RocksDB and have outbox entries when dedup gate cannot function"
    - "Route_query returns results even when metadata has no timestamp_ms (fail-open for StaleFilter)"
  artifacts:
    - path: "crates/e2e-tests/tests/fail_open_test.rs"
      provides: "E2E fail-open tests proving TEST-03"
      min_lines: 80
  key_links:
    - from: "crates/e2e-tests/tests/fail_open_test.rs"
      to: "MemoryServiceImpl.ingest_event()"
      via: "ingest with NoveltyChecker(embedder=None)"
      pattern: "service\\.ingest_event"
    - from: "crates/e2e-tests/tests/fail_open_test.rs"
      to: "Storage.get_stats()"
      via: "verify all events stored"
      pattern: "storage\\.get_stats"
---

<objective>
E2E fail-open tests proving the system degrades gracefully when dedup gate or stale filter encounters errors (TEST-03).

Purpose: Validate that when the embedder fails to load (None), all events still ingest normally with outbox entries, and when StaleFilter receives results without timestamps, it returns them unmodified.
Output: `fail_open_test.rs` with 2-3 tests covering dedup fail-open and staleness fail-open.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-e2e-validation/38-CONTEXT.md
@.planning/phases/38-e2e-validation/38-RESEARCH.md

@crates/e2e-tests/src/lib.rs
@crates/e2e-tests/tests/pipeline_test.rs
@crates/memory-service/src/ingest.rs
@crates/memory-service/src/novelty.rs
@crates/memory-retrieval/src/stale_filter.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fail_open_test.rs with dedup and staleness fail-open E2E tests (TEST-03)</name>
  <files>crates/e2e-tests/tests/fail_open_test.rs</files>
  <action>
Create `crates/e2e-tests/tests/fail_open_test.rs` with the following tests. All use `#[tokio::test]` and `pretty_assertions`.

**Test 1: `test_fail_open_embedder_disabled_events_still_stored`**
Per user decision: embedder disabled via NoveltyChecker with embedder=None (natural fail path when CandleEmbedder fails to load).
- Create TestHarness
- Create NoveltyChecker with embedder=None: `NoveltyChecker::new(None, None, DedupConfig { enabled: true, ..Default::default() })`
- Create MemoryServiceImpl, call `service.set_novelty_checker(checker)`
- Ingest 5 events via `service.ingest_event()` with distinct event_ids and texts
- Assert ALL ingest_event calls return Ok (no error -- per user decision)
- Assert `deduplicated=false` for all responses (fail-open means events pass through)
- Assert `created=true` for all responses
- Assert `harness.storage.get_stats().unwrap().event_count == 5` (all stored in RocksDB)
- Assert `harness.storage.get_outbox_entries(0, 100).unwrap().len() == 5` (all have outbox entries, proving normal ingest path)

**Test 2: `test_fail_open_embedder_error_events_pass_through`**
- Create a `FailingEmbedder` struct (defined locally) that implements `EmbedderTrait` and always returns `Err("model load failed".to_string())` from `embed()`.
- Create NoveltyChecker with `Some(failing_embedder)`, no vector_index, DedupConfig enabled
- Create MemoryServiceImpl with this checker
- Ingest 3 events
- Assert all return Ok with `deduplicated=false` (fail-open: embed error means event passes through)
- Assert all 3 events in storage with outbox entries

**Test 3: `test_fail_open_staleness_no_timestamp_returns_results`**
Per user decision: StaleFilter fail-open tested when metadata has no timestamp_ms.
- Create TestHarness
- Ingest events via `ingest_events()` (the helper that bypasses dedup)
- Build TOC and index into BM25 following `pipeline_test.rs` pattern
- Create RetrievalHandler with StalenessConfig enabled (`enabled: true, half_life_days: 14.0, max_penalty: 0.30`)
- Call `handler.route_query()` with a matching query
- Assert `has_results=true` -- the StaleFilter should gracefully handle results that may lack timestamp_ms metadata rather than erroring out
- Assert results are non-empty (proving the filter passed them through)

Use imports: `use std::sync::Arc`, `use tonic::Request`, `use e2e_tests::{TestHarness, create_proto_event, create_test_events, ingest_events, build_toc_segment}`, `use memory_service::{MemoryServiceImpl, NoveltyChecker, pb::{IngestEventRequest, memory_service_server::MemoryService}}`, `use memory_types::config::{DedupConfig, StalenessConfig}`, `use memory_search::{SearchIndex, SearchIndexConfig, SearchIndexer, TeleportSearcher}`, `use memory_service::{RetrievalHandler, pb::RouteQueryRequest}`.

For `FailingEmbedder`, define in the test file:
```rust
struct FailingEmbedder;

#[async_trait::async_trait]
impl memory_service::novelty::EmbedderTrait for FailingEmbedder {
    async fn embed(&self, _text: &str) -> Result<Vec<f32>, String> {
        Err("model load failed".to_string())
    }
}
```
  </action>
  <verify>
`cargo test -p e2e-tests --test fail_open_test` -- all 3 tests pass. No warnings from clippy.
  </verify>
  <done>
Three E2E tests prove: (1) embedder=None -> all events ingest normally with outbox entries, (2) embedder errors -> events pass through unchanged, (3) StaleFilter returns results even without timestamp metadata. TEST-03 satisfied.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p e2e-tests --test fail_open_test` passes all tests
- `cargo clippy -p e2e-tests --all-targets -- -D warnings` clean
- All events stored in RocksDB with outbox entries when dedup gate is non-functional
- No errors returned from ingest_event when embedder is disabled or failing
</verification>

<success_criteria>
- Dedup gate fail-open proven: disabled embedder and erroring embedder both result in normal ingest
- StaleFilter fail-open proven: results returned even without timestamp metadata
- All events preserved in storage regardless of dedup gate state
- TEST-03 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/38-e2e-validation/38-03-SUMMARY.md`
</output>
