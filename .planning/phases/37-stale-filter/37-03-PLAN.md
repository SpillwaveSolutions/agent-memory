---
phase: 37-stale-filter
plan: 03
type: execute
wave: 1
depends_on: ["37-02"]
files_modified:
  - crates/memory-service/src/server.rs
  - crates/memory-service/src/ingest.rs
  - crates/memory-daemon/src/commands.rs
autonomous: true
gap_closure: true
requirements: [RETRV-04]

must_haves:
  truths:
    - "StalenessConfig loaded from config.toml is propagated to RetrievalHandler at daemon startup"
    - "All MemoryServiceImpl constructors that use RetrievalHandler pass the settings-derived StalenessConfig"
    - "Changing staleness settings in config.toml affects runtime behavior without code changes"
  artifacts:
    - path: "crates/memory-service/src/server.rs"
      provides: "run_server_with_scheduler accepts StalenessConfig parameter"
      contains: "staleness_config: StalenessConfig"
    - path: "crates/memory-service/src/ingest.rs"
      provides: "with_scheduler passes StalenessConfig to RetrievalHandler"
      contains: "staleness_config"
    - path: "crates/memory-daemon/src/commands.rs"
      provides: "start_daemon passes settings.staleness to run_server_with_scheduler"
      contains: "settings.staleness"
  key_links:
    - from: "crates/memory-daemon/src/commands.rs"
      to: "crates/memory-service/src/server.rs"
      via: "settings.staleness.clone() passed to run_server_with_scheduler"
      pattern: "settings\\.staleness"
    - from: "crates/memory-service/src/server.rs"
      to: "crates/memory-service/src/ingest.rs"
      via: "StalenessConfig forwarded to MemoryServiceImpl::with_scheduler"
      pattern: "with_scheduler.*staleness"
    - from: "crates/memory-service/src/ingest.rs"
      to: "crates/memory-service/src/retrieval.rs"
      via: "with_scheduler creates RetrievalHandler with config-derived StalenessConfig"
      pattern: "RetrievalHandler.*staleness_config"
---

<objective>
Close RETRV-04 gap: Wire StalenessConfig from Settings through daemon startup to RetrievalHandler.

Purpose: The StalenessConfig struct exists with correct defaults and the RetrievalHandler accepts it via with_services(), but the daemon startup path (start_daemon -> run_server_with_scheduler -> MemoryServiceImpl::with_scheduler) hardcodes StalenessConfig::default() instead of passing the value loaded from config.toml. This means users cannot tune staleness parameters at runtime.

Output: All daemon startup paths propagate settings.staleness to RetrievalHandler, making config.toml staleness section effective.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-stale-filter/37-VERIFICATION.md
@crates/memory-service/src/server.rs
@crates/memory-service/src/ingest.rs
@crates/memory-service/src/retrieval.rs
@crates/memory-daemon/src/commands.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StalenessConfig parameter to run_server_with_scheduler and MemoryServiceImpl::with_scheduler</name>
  <files>crates/memory-service/src/server.rs, crates/memory-service/src/ingest.rs</files>
  <action>
1. In server.rs, add `use memory_types::config::StalenessConfig;` import (if not already present).

2. In server.rs `run_server_with_scheduler` function (line ~115), add a `staleness_config: StalenessConfig` parameter after `novelty_checker`:
   ```
   pub async fn run_server_with_scheduler<F>(
       addr: SocketAddr,
       storage: Arc<Storage>,
       scheduler: SchedulerService,
       shutdown_signal: F,
       novelty_checker: Option<Arc<NoveltyChecker>>,
       staleness_config: StalenessConfig,
   ) -> Result<(), Box<dyn std::error::Error + Send + Sync>>
   ```

3. In server.rs line ~149, change `MemoryServiceImpl::with_scheduler(storage, scheduler.clone())` to `MemoryServiceImpl::with_scheduler(storage, scheduler.clone(), staleness_config)`.

4. In ingest.rs `with_scheduler` method (line ~87), add `staleness_config: StalenessConfig` parameter and use it:
   ```
   pub fn with_scheduler(storage: Arc<Storage>, scheduler: Arc<SchedulerService>, staleness_config: StalenessConfig) -> Self {
       let retrieval = Arc::new(RetrievalHandler::with_services(
           storage.clone(),
           None,
           None,
           None,
           staleness_config,
       ));
       ...
   ```
   Note: This replaces `RetrievalHandler::new(storage.clone())` with `RetrievalHandler::with_services(...)` so the config is passed through.

5. Also update all other `with_*` constructors in ingest.rs that currently hardcode `StalenessConfig::default()`. Each should accept `staleness_config: StalenessConfig` as a parameter instead:
   - `with_scheduler_and_search` (line ~106)
   - `with_search` (line ~131)
   - `with_vector` (line ~157)
   - `with_topics` (line ~182)
   - `with_full_services` (line ~208, the one with searcher + vector)
   - `with_all_services` (line ~238, the one with searcher + vector + topics)

   For each, replace `StalenessConfig::default()` in the `RetrievalHandler::with_services(...)` call with the passed-in `staleness_config` parameter.

6. Update the `run_server_with_scheduler` re-export in `crates/memory-service/src/lib.rs` if needed (it likely uses a wildcard or explicit re-export that already covers the function signature change).

7. Run `cargo build -p memory-service` to verify the changes compile. Fix any compilation errors from callers that need updating.
  </action>
  <verify>
`cargo build -p memory-service` compiles without errors. The function signatures now accept StalenessConfig.
  </verify>
  <done>
run_server_with_scheduler and all MemoryServiceImpl constructors accept StalenessConfig parameter instead of hardcoding default.
  </done>
</task>

<task type="auto">
  <name>Task 2: Propagate settings.staleness from start_daemon through to server</name>
  <files>crates/memory-daemon/src/commands.rs</files>
  <action>
1. In commands.rs `start_daemon` function, the `settings` variable is already loaded (line ~307). After the `novelty_checker` setup block, pass `settings.staleness.clone()` as the new final argument to `run_server_with_scheduler`:
   ```
   let result = run_server_with_scheduler(
       addr,
       storage,
       scheduler,
       shutdown_signal,
       novelty_checker,
       settings.staleness.clone(),
   ).await;
   ```

2. Add an info log line near the other config info logs (around line ~334) to confirm staleness settings are loaded:
   ```
   info!("  Staleness filter: enabled={}, half_life={}d, max_penalty={}",
       settings.staleness.enabled, settings.staleness.half_life_days, settings.staleness.max_penalty);
   ```

3. Run `cargo build -p memory-daemon` to verify everything compiles.

4. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings` to ensure no warnings.

5. Run `cargo test --workspace --all-features` to ensure all existing tests pass. Some tests in ingest.rs may call the old `with_scheduler` signature without the StalenessConfig parameter -- fix these by passing `StalenessConfig::default()` in test code (the tests don't need custom staleness config).
  </action>
  <verify>
`cargo clippy --workspace --all-targets --all-features -- -D warnings` passes. `cargo test --workspace --all-features` passes. `grep -rn "StalenessConfig::default()" crates/memory-service/src/ingest.rs` returns zero matches (only test code may use defaults).
  </verify>
  <done>
settings.staleness is propagated from config.toml through start_daemon -> run_server_with_scheduler -> MemoryServiceImpl::with_scheduler -> RetrievalHandler. No production code path uses StalenessConfig::default(). RETRV-04 gap is closed.
  </done>
</task>

</tasks>

<verification>
1. `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
2. `cargo test --workspace --all-features` passes
3. `grep -rn "StalenessConfig::default()" crates/memory-service/src/ingest.rs` shows zero matches in non-test code
4. `grep -rn "settings.staleness" crates/memory-daemon/src/commands.rs` shows the config being passed through
5. `grep -n "staleness_config" crates/memory-service/src/server.rs` shows the parameter in run_server_with_scheduler
</verification>

<success_criteria>
- StalenessConfig loaded from config.toml reaches RetrievalHandler at daemon startup
- All MemoryServiceImpl constructors accept StalenessConfig (no hardcoded defaults in production paths)
- All tests pass, no clippy warnings
- RETRV-04 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/37-stale-filter/37-03-SUMMARY.md`
</output>
