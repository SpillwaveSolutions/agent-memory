---
phase: 54-daily-markdown-export
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - crates/memory-cli/src/cli.rs
  - crates/memory-cli/src/commands/mod.rs
  - crates/memory-cli/src/commands/daily.rs
  - crates/memory-cli/src/main.rs
autonomous: true
requirements: [DAILY-01, DAILY-02, DAILY-03, DAILY-04, DAILY-05]
must_haves:
  truths:
    - "Running `memory daily` produces a `memory/YYYY-MM-DD.md` file for today"
    - "Running `memory daily --range 7d` produces one markdown file per active day"
    - "Each markdown file includes session markers with agent names, summary bullets, keywords, and grip excerpts"
    - "Days without rollup include a 'summary pending' note instead of bullets"
    - "Days without events produce no files"
    - "Each file has a footer with 'derived view' notice and export timestamp"
  artifacts:
    - path: "crates/memory-cli/src/cli.rs"
      provides: "Daily variant in Commands enum + DailyArgs struct"
      contains: "Daily(DailyArgs)"
    - path: "crates/memory-cli/src/commands/daily.rs"
      provides: "Daily command implementation with markdown rendering"
      contains: "pub async fn run"
    - path: "crates/memory-cli/src/commands/mod.rs"
      provides: "daily module declaration"
      contains: "pub mod daily"
    - path: "crates/memory-cli/src/main.rs"
      provides: "Daily command dispatch"
      contains: "Commands::Daily"
  key_links:
    - from: "crates/memory-cli/src/commands/daily.rs"
      to: "memory_client::MemoryClient"
      via: "client.export_daily() call"
      pattern: "client\\.export_daily"
    - from: "crates/memory-cli/src/commands/daily.rs"
      to: "filesystem"
      via: "std::fs::write for each day"
      pattern: "fs::write"
    - from: "crates/memory-cli/src/main.rs"
      to: "commands/daily.rs"
      via: "Commands::Daily dispatch"
      pattern: "Commands::Daily"
---

<objective>
Add the `memory daily` CLI subcommand that produces human-readable markdown daily files from the ExportDaily RPC response.

Purpose: Users get browsable "warm safety blanket" files they can check into GitHub -- OpenClaw-style daily markdown files showing sessions, summaries, keywords, and key moments.

Output: New `daily` subcommand with `--range` and `--dir` flags, markdown rendering logic, and CLI argument tests.
</objective>

<execution_context>
@/Users/richardhightower/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardhightower/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-daily-markdown-export/54-CONTEXT.md
@.planning/phases/54-daily-markdown-export/54-RESEARCH.md
@.planning/phases/54-daily-markdown-export/54-01-SUMMARY.md
@docs/superpowers/specs/2026-03-23-memory-export-import-design.md

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From memory_client (after Plan 01):
```rust
pub struct ExportDailyResult {
    pub days: Vec<DayExport>,
}

pub use memory_service::pb::DayExport;

// DayExport fields (from proto):
// - date: String ("YYYY-MM-DD")
// - day_node: Option<TocNode>
// - segments: Vec<TocNode>
// - events: Vec<Event>
// - grips: Vec<Grip>
// - has_rollup: bool
```

From existing CLI patterns:
```rust
// cli.rs Commands enum -- add Daily(DailyArgs) variant
pub enum Commands {
    Search(SearchArgs),
    Context(ContextArgs),
    Add(AddArgs),
    Timeline(TimelineArgs),
    Summary(SummaryArgs),
    Recall(RecallArgs),
    // Add: Daily(DailyArgs),
}

// main.rs dispatch pattern:
Commands::Timeline(args) => commands::timeline::run(args, &cli.global).await,

// client.rs helper:
pub async fn connect_client(endpoint: &str) -> anyhow::Result<MemoryClient>;

// timeline.rs parse_range (reusable):
pub(crate) fn parse_range(range: &str) -> (i64, i64);
```

From proto (Event fields used for session grouping):
```rust
// Event.session_id: String
// Event.agent: Option<String>
// Event.timestamp_ms: i64
// Event.event_type: i32 (1=session_start, 8=session_end)
```

From proto (TocNode fields used for summary rendering):
```rust
// TocNode.bullets: Vec<TocBullet>
// TocBullet.text: String
// TocBullet.grip_ids: Vec<String>
// TocNode.keywords: Vec<String>
// TocNode.summary: Option<String>
```

From proto (Grip fields used for excerpt rendering):
```rust
// Grip.grip_id: String
// Grip.excerpt: String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DailyArgs to CLI and create daily command module with markdown rendering</name>
  <files>
    crates/memory-cli/src/cli.rs,
    crates/memory-cli/src/commands/mod.rs,
    crates/memory-cli/src/commands/daily.rs,
    crates/memory-cli/src/main.rs
  </files>
  <read_first>
    - crates/memory-cli/src/cli.rs (full file -- Commands enum pattern)
    - crates/memory-cli/src/main.rs (full file -- dispatch pattern)
    - crates/memory-cli/src/commands/mod.rs (module declarations)
    - crates/memory-cli/src/commands/timeline.rs (parse_range pattern, event mapping)
    - crates/memory-cli/src/commands/summary.rs (TOC navigation pattern)
    - crates/memory-cli/src/client.rs (connect_client helper)
    - crates/memory-client/src/client.rs (export_daily method, DayExport re-export)
  </read_first>
  <behavior>
    - Test: `Cli::try_parse_from(["memory", "daily"])` succeeds with default dir="./memory" and range=None
    - Test: `Cli::try_parse_from(["memory", "daily", "--range", "7d"])` parses range as Some("7d")
    - Test: `Cli::try_parse_from(["memory", "daily", "--dir", "/tmp/out"])` parses dir as "/tmp/out"
    - Test: `compute_date_range(None)` returns (today, today) as "YYYY-MM-DD" strings
    - Test: `compute_date_range(Some("7d"))` returns (today-6d, today) as "YYYY-MM-DD" strings
    - Test: `render_day_markdown` with has_rollup=true produces heading, summary bullets, keywords, grip excerpts, footer
    - Test: `render_day_markdown` with has_rollup=false produces heading, "Summary pending" note, footer
    - Test: `render_day_markdown` footer contains "derived view" and "Exported from agent-memory"
    - Test: `group_events_by_session` groups events by session_id, tracks min/max timestamps and agent name
  </behavior>
  <action>
**1. cli.rs -- Add DailyArgs and Daily variant:**

Add to imports if needed, then add the struct and enum variant:

```rust
/// Export daily markdown files from memory.
Daily(DailyArgs),
```

```rust
/// Arguments for the `daily` subcommand.
#[derive(Parser, Debug)]
pub struct DailyArgs {
    /// Time range for export (e.g., "7d", "30d"). Default: today only.
    #[arg(long)]
    pub range: Option<String>,

    /// Output directory for markdown files.
    #[arg(long, default_value = "./memory")]
    pub dir: String,
}
```

Add a CLI parse test in the existing `mod tests` block:
```rust
#[test]
fn test_parse_daily() {
    let cli = Cli::try_parse_from(["memory", "daily"]).unwrap();
    match cli.command {
        Commands::Daily(args) => {
            assert!(args.range.is_none());
            assert_eq!(args.dir, "./memory");
        }
        _ => panic!("Expected Daily command"),
    }
}

#[test]
fn test_parse_daily_with_options() {
    let cli = Cli::try_parse_from(["memory", "daily", "--range", "7d", "--dir", "/tmp/out"]).unwrap();
    match cli.command {
        Commands::Daily(args) => {
            assert_eq!(args.range.as_deref(), Some("7d"));
            assert_eq!(args.dir, "/tmp/out");
        }
        _ => panic!("Expected Daily command"),
    }
}
```

Also update the `test_all_subcommands_parse` test to include `vec!["memory", "daily"]`.

**2. commands/mod.rs -- Add module:**
```rust
pub mod daily;
```

**3. main.rs -- Add dispatch:**
```rust
Commands::Daily(args) => commands::daily::run(args, &cli.global).await,
```

**4. commands/daily.rs -- Full implementation:**

Create this new file with the following structure:

```rust
//! `memory daily` command -- export daily markdown files from memory.

use anyhow::Result;
use chrono::Utc;
use std::collections::HashMap;
use std::path::Path;

use memory_client::DayExport;

use crate::cli::{DailyArgs, GlobalArgs};
use crate::client::connect_client;

/// A group of events belonging to one session.
struct SessionGroup {
    session_id: String,
    agent: String,
    start_ms: i64,
    end_ms: i64,
    event_count: usize,
}

/// Run the daily export command.
pub async fn run(args: DailyArgs, global: &GlobalArgs) -> Result<()> {
    let mut client = connect_client(&global.endpoint).await?;
    let (start, end) = compute_date_range(&args.range);
    let result = client.export_daily(&start, &end).await?;

    std::fs::create_dir_all(&args.dir)?;

    if result.days.is_empty() {
        eprintln!("No days with events in range {} to {}", start, end);
        return Ok(());
    }

    for day in &result.days {
        let md = render_day_markdown(day);
        let path = format!("{}/{}.md", args.dir, day.date);
        std::fs::write(&path, &md)?;
        eprintln!("Wrote {}", path);
    }

    eprintln!("Exported {} day(s)", result.days.len());
    Ok(())
}

/// Compute start/end date strings from the --range flag.
///
/// None => today only. Some("7d") => last 7 days. Some("30d") => last 30 days.
fn compute_date_range(range: &Option<String>) -> (String, String) {
    let today = Utc::now().date_naive();
    match range {
        None => {
            let s = today.format("%Y-%m-%d").to_string();
            (s.clone(), s)
        }
        Some(r) => {
            let days = parse_range_to_days(r);
            let start = today - chrono::Duration::days(days - 1);
            (
                start.format("%Y-%m-%d").to_string(),
                today.format("%Y-%m-%d").to_string(),
            )
        }
    }
}

/// Parse a range string like "7d" or "30d" into number of days.
fn parse_range_to_days(range: &str) -> i64 {
    let range = range.trim();
    if let Some(stripped) = range.strip_suffix('d') {
        stripped.parse::<i64>().unwrap_or(7).max(1)
    } else if let Some(stripped) = range.strip_suffix('w') {
        stripped.parse::<i64>().unwrap_or(1).max(1) * 7
    } else {
        range.parse::<i64>().unwrap_or(7).max(1)
    }
}

/// Render a DayExport into markdown.
fn render_day_markdown(day: &DayExport) -> String {
    let mut md = String::new();

    // Title
    md.push_str(&format!("# {}\n\n", day.date));

    // Summary section (from day node if rollup complete)
    if day.has_rollup {
        if let Some(ref node) = day.day_node {
            // Summary bullets
            if !node.bullets.is_empty() {
                md.push_str("## Summary\n\n");
                for bullet in &node.bullets {
                    md.push_str(&format!("- {}\n", bullet.text));
                }
                md.push('\n');
            }
            // Keywords
            if !node.keywords.is_empty() {
                md.push_str(&format!("**Keywords:** {}\n\n", node.keywords.join(", ")));
            }
        }
    } else {
        md.push_str("*Summary pending -- day rollup not yet complete*\n\n");
    }

    // Sessions section (grouped from events by session_id)
    let sessions = group_events_by_session(&day.events);
    if !sessions.is_empty() {
        md.push_str("## Sessions\n\n");
        for (i, session) in sessions.iter().enumerate() {
            let start_time = format_timestamp_ms(session.start_ms);
            let end_time = format_timestamp_ms(session.end_ms);
            let agent_label = if session.agent.is_empty() {
                String::new()
            } else {
                format!(" [agent: {}]", session.agent)
            };
            md.push_str(&format!(
                "### Session {} ({} -- {}, {} events){}\n\n",
                i + 1,
                start_time,
                end_time,
                session.event_count,
                agent_label,
            ));

            // Find matching segment summary for this session's time range
            for seg in &day.segments {
                if seg.start_time_ms <= session.end_ms && seg.end_time_ms >= session.start_ms {
                    if let Some(ref summary) = seg.summary {
                        if !summary.is_empty() {
                            md.push_str(&format!("**Summary:** {}\n\n", summary));
                        }
                    }
                    // Segment bullets as key points
                    if !seg.bullets.is_empty() {
                        md.push_str("**Key points:**\n");
                        for bullet in &seg.bullets {
                            md.push_str(&format!("- {}\n", bullet.text));
                        }
                        md.push('\n');
                    }
                    // Segment keywords
                    if !seg.keywords.is_empty() {
                        md.push_str(&format!("**Keywords:** {}\n\n", seg.keywords.join(", ")));
                    }
                }
            }
        }
    }

    // Grip excerpts (key moments)
    if !day.grips.is_empty() {
        md.push_str("## Key Moments\n\n");
        for grip in &day.grips {
            if !grip.excerpt.is_empty() {
                md.push_str(&format!("> {}\n> -- {}\n\n", grip.excerpt, grip.grip_id));
            }
        }
    }

    // Footer (DAILY-05)
    md.push_str("---\n\n");
    md.push_str(&format!(
        "*Exported from agent-memory at {} -- this file is a derived view*\n",
        Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
    ));

    md
}

/// Group proto events by session_id, tracking start/end times and agent.
fn group_events_by_session(
    events: &[memory_service::pb::Event],
) -> Vec<SessionGroup> {
    // Use Vec to preserve insertion order (first-seen session_id order)
    let mut session_map: HashMap<String, usize> = HashMap::new();
    let mut sessions: Vec<SessionGroup> = Vec::new();

    for event in events {
        let sid = &event.session_id;
        if let Some(&idx) = session_map.get(sid) {
            let s = &mut sessions[idx];
            s.start_ms = s.start_ms.min(event.timestamp_ms);
            s.end_ms = s.end_ms.max(event.timestamp_ms);
            s.event_count += 1;
            // Use first non-empty agent
            if s.agent.is_empty() {
                if let Some(ref agent) = event.agent {
                    if !agent.is_empty() {
                        s.agent = agent.clone();
                    }
                }
            }
        } else {
            let idx = sessions.len();
            session_map.insert(sid.clone(), idx);
            sessions.push(SessionGroup {
                session_id: sid.clone(),
                agent: event.agent.clone().unwrap_or_default(),
                start_ms: event.timestamp_ms,
                end_ms: event.timestamp_ms,
                event_count: 1,
            });
        }
    }

    sessions
}

/// Format a timestamp_ms as "HH:MM" for display.
fn format_timestamp_ms(ms: i64) -> String {
    use chrono::TimeZone;
    match Utc.timestamp_millis_opt(ms) {
        chrono::LocalResult::Single(dt) => dt.format("%H:%M").to_string(),
        _ => "??:??".to_string(),
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use memory_service::pb::{
        Event as ProtoEvent, Grip as ProtoGrip, TocBullet as ProtoTocBullet,
        TocNode as ProtoTocNode,
    };

    fn make_day_export(has_rollup: bool) -> DayExport {
        let day_node = if has_rollup {
            Some(ProtoTocNode {
                node_id: "toc:day:2026-03-23".into(),
                title: "2026-03-23".into(),
                bullets: vec![
                    ProtoTocBullet {
                        text: "Implemented RRF fusion".into(),
                        grip_ids: vec!["grip:001".into()],
                    },
                    ProtoTocBullet {
                        text: "Added CLI binary".into(),
                        grip_ids: vec![],
                    },
                ],
                keywords: vec!["RRF".into(), "CLI".into()],
                ..Default::default()
            })
        } else {
            None
        };

        DayExport {
            date: "2026-03-23".into(),
            day_node,
            segments: vec![],
            events: vec![
                ProtoEvent {
                    event_id: "evt-1".into(),
                    session_id: "sess-1".into(),
                    timestamp_ms: 1711180800000, // 2026-03-23 08:00 UTC
                    agent: Some("claude".into()),
                    ..Default::default()
                },
                ProtoEvent {
                    event_id: "evt-2".into(),
                    session_id: "sess-1".into(),
                    timestamp_ms: 1711184400000, // 2026-03-23 09:00 UTC
                    agent: Some("claude".into()),
                    ..Default::default()
                },
            ],
            grips: vec![ProtoGrip {
                grip_id: "grip:001".into(),
                excerpt: "RRF is parameter-free".into(),
                ..Default::default()
            }],
            has_rollup,
        }
    }

    #[test]
    fn test_render_day_with_rollup() {
        let day = make_day_export(true);
        let md = render_day_markdown(&day);
        assert!(md.contains("# 2026-03-23"));
        assert!(md.contains("## Summary"));
        assert!(md.contains("- Implemented RRF fusion"));
        assert!(md.contains("- Added CLI binary"));
        assert!(md.contains("**Keywords:** RRF, CLI"));
        assert!(md.contains("## Key Moments"));
        assert!(md.contains("> RRF is parameter-free"));
        assert!(md.contains("derived view"));
        assert!(md.contains("Exported from agent-memory"));
    }

    #[test]
    fn test_render_day_without_rollup() {
        let day = make_day_export(false);
        let md = render_day_markdown(&day);
        assert!(md.contains("# 2026-03-23"));
        assert!(md.contains("Summary pending"));
        assert!(!md.contains("## Summary"));
        assert!(md.contains("derived view"));
    }

    #[test]
    fn test_footer_contains_derived_view() {
        let day = make_day_export(true);
        let md = render_day_markdown(&day);
        assert!(md.contains("this file is a derived view"));
        assert!(md.contains("Exported from agent-memory at"));
    }

    #[test]
    fn test_group_events_by_session() {
        let events = vec![
            ProtoEvent {
                session_id: "sess-1".into(),
                agent: Some("claude".into()),
                timestamp_ms: 1000,
                ..Default::default()
            },
            ProtoEvent {
                session_id: "sess-1".into(),
                agent: Some("claude".into()),
                timestamp_ms: 2000,
                ..Default::default()
            },
            ProtoEvent {
                session_id: "sess-2".into(),
                agent: Some("gemini".into()),
                timestamp_ms: 3000,
                ..Default::default()
            },
        ];
        let sessions = group_events_by_session(&events);
        assert_eq!(sessions.len(), 2);
        assert_eq!(sessions[0].session_id, "sess-1");
        assert_eq!(sessions[0].agent, "claude");
        assert_eq!(sessions[0].start_ms, 1000);
        assert_eq!(sessions[0].end_ms, 2000);
        assert_eq!(sessions[0].event_count, 2);
        assert_eq!(sessions[1].session_id, "sess-2");
        assert_eq!(sessions[1].agent, "gemini");
        assert_eq!(sessions[1].event_count, 1);
    }

    #[test]
    fn test_compute_date_range_today() {
        let (start, end) = compute_date_range(&None);
        assert_eq!(start, end);
        // Should be today's date
        let today = Utc::now().date_naive().format("%Y-%m-%d").to_string();
        assert_eq!(start, today);
    }

    #[test]
    fn test_compute_date_range_7d() {
        let (start, end) = compute_date_range(&Some("7d".to_string()));
        let today = Utc::now().date_naive();
        let expected_start = (today - chrono::Duration::days(6)).format("%Y-%m-%d").to_string();
        assert_eq!(start, expected_start);
        assert_eq!(end, today.format("%Y-%m-%d").to_string());
    }

    #[test]
    fn test_parse_range_to_days() {
        assert_eq!(parse_range_to_days("7d"), 7);
        assert_eq!(parse_range_to_days("30d"), 30);
        assert_eq!(parse_range_to_days("1w"), 7);
        assert_eq!(parse_range_to_days("2w"), 14);
    }
}
```

NOTE on import path: The exact import for `memory_service::pb::Event` (used in `group_events_by_session`) depends on what `memory_client` re-exports. Check the `memory_client` crate's `lib.rs` to see what proto types are re-exported. If `DayExport` is re-exported but not the `pb` module, you may need to use `memory_service::pb::Event` directly or adjust the import. The key is: the `events` field on `DayExport` is `Vec<memory_service::pb::Event>` -- use whatever path compiles.

Decision (Claude's discretion): Always overwrite existing daily files (simpler, idempotent -- files are derived views). Grip excerpts use blockquote style (`> excerpt text`). Include event counts per session in the session header.
  </action>
  <verify>
    <automated>cd /Users/richardhightower/clients/spillwave/src/agent-memory && cargo test -p memory-cli --lib -- daily 2>&1 | tail -20 && cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Daily(DailyArgs)" crates/memory-cli/src/cli.rs
    - grep -q "pub mod daily" crates/memory-cli/src/commands/mod.rs
    - grep -q "Commands::Daily" crates/memory-cli/src/main.rs
    - grep -q "pub async fn run" crates/memory-cli/src/commands/daily.rs
    - grep -q "render_day_markdown" crates/memory-cli/src/commands/daily.rs
    - grep -q "group_events_by_session" crates/memory-cli/src/commands/daily.rs
    - grep -q "derived view" crates/memory-cli/src/commands/daily.rs
    - grep -q "Summary pending" crates/memory-cli/src/commands/daily.rs
    - cargo test -p memory-cli --lib -- daily passes all tests
  </acceptance_criteria>
  <done>
`memory daily` subcommand is fully wired: DailyArgs with --range and --dir flags, dispatch in main.rs, daily.rs with run() function that calls export_daily RPC and writes markdown files. Markdown includes: heading with date, summary bullets and keywords (when rollup complete), "Summary pending" note (when not), sessions grouped by session_id with agent names and event counts, grip excerpts in blockquote style, and footer with "derived view" notice and export timestamp. All unit tests pass for markdown rendering, session grouping, date range parsing, and CLI argument parsing. Workspace compiles clean with clippy.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p memory-cli --lib -- daily` -- all daily-specific unit tests pass
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` -- no warnings
- `cargo test --workspace --all-features` -- all existing + new tests pass
- `cargo fmt --all -- --check` -- formatting clean
- Manually verify: `memory daily --help` shows usage with --range and --dir flags
</verification>

<success_criteria>
The `memory daily` CLI command exists with `--range` and `--dir` flags. When connected to a running daemon, it calls `ExportDaily` RPC and writes one `YYYY-MM-DD.md` file per active day. Markdown format includes: date heading, summary bullets + keywords (or "Summary pending" note for unrolled days), session markers with agent names and event counts, grip excerpts as blockquotes, and a footer with "derived view" notice. Days with no events produce no files (DAILY-04). Unit tests cover rendering for both rolled-up and pending days, session grouping, date range parsing, and CLI argument parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/54-daily-markdown-export/54-02-SUMMARY.md`
</output>
